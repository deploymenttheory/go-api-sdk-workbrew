package devicegroups

const (
	// EndpointDeviceGroupsJSON is the endpoint for device groups in JSON format
	EndpointDeviceGroupsJSON = "/device_groups.json"

	// EndpointDeviceGroupsCSV is the endpoint for device groups in CSV format
	EndpointDeviceGroupsCSV = "/device_groups.csv"
)
