package formulae

const (
	// EndpointFormulaeJSON is the endpoint for formulae in JSON format
	EndpointFormulaeJSON = "/formulae.json"

	// EndpointFormulaeCSV is the endpoint for formulae in CSV format
	EndpointFormulaeCSV = "/formulae.csv"
)
